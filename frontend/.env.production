# Leave empty for the docker compose deployment — nginx proxies /api/* to the backend service.
# For a real production deployment behind a custom domain, set to e.g. https://api.your-domain.com
VITE_API_URL=
